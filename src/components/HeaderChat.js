import React from 'react';

const HeaderChat = ({ room }) => {
  return <div style={{ backgroundColor: 'blue', padding: '1em 2em', color: 'white' }}>nama room: {room}</div>;
};

export default HeaderChat;
